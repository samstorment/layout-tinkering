---
    import type { HTMLAttributes } from "astro/types";

    interface Props extends HTMLAttributes<'a'> {
        href: string;
    }

    const { href, ...rest } = Astro.props;
---

<a class="hover:text-white hover:underline whitespace-nowrap" href={href} {...rest}>
    <slot />
</a>